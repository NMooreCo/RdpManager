<UserControl x:Class="RdpManager.Views.ServersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:RdpManager.Models"
             xmlns:converters="clr-namespace:RdpManager.Converters">

    <UserControl.Resources>
        <converters:NullToFontWeightConverter x:Key="NullToFontWeightConverter" NullFontWeight="SemiBold" NotNullFontWeight="Normal" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- Expand/Collapse Toggle Button Style -->
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" Width="16" Height="16">
                            <Path x:Name="ExpandPath"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z"
                                  Fill="{DynamicResource App.Text.Secondary}"
                                  Width="10"
                                  Height="10"
                                  Stretch="Uniform"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ExpandPath" Property="Data" Value="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ExpandPath" Property="Fill" Value="{DynamicResource App.Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Error Banner (visible only when server load fails) -->
        <Border Grid.Row="0"
                Background="#33D32F2F"
                BorderBrush="#D32F2F"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,8"
                Visibility="{Binding ServerLoadError, Converter={StaticResource NullToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <Path Data="M13,14H11V10H13M13,18H11V16H13M1,21H23L12,2L1,21Z"
                      Fill="#D32F2F" Width="16" Height="16" Stretch="Uniform"
                      VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ServerLoadError}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource App.Text.Primary}"
                           FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Toolbar with Search -->
        <Border Grid.Row="1" Style="{StaticResource Toolbar}" CornerRadius="8,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Border Grid.Column="0" BorderBrush="{DynamicResource App.Border.Divider}" BorderThickness="1" Background="Transparent" CornerRadius="6" Margin="0,0,12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0" Data="M21,21L15.5,15.5L17.5,13.5L23,19L21,21M9,3A6,6 0 0,1 15,9A6,6 0 0,1 9,15A6,6 0 0,1 3,9A6,6 0 0,1 9,3M9,5A4,4 0 0,0 5,9A4,4 0 0,0 9,13A4,4 0 0,0 13,9A4,4 0 0,0 9,5Z"
                              Fill="{DynamicResource App.Text.Secondary}" Width="14" Height="14" Stretch="Uniform" Margin="10,0,0,0"/>
                        <TextBox x:Name="SearchBox"
                                 Grid.Column="1"
                                 Height="34"
                                 Padding="8,7"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="13"
                                 VerticalContentAlignment="Center"
                                 TextChanged="SearchBox_TextChanged">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Grid>
                                                    <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                                                    <TextBlock Text="Search servers..."
                                                               Foreground="{DynamicResource App.Text.Secondary}"
                                                               VerticalAlignment="Center"
                                                               IsHitTestVisible="False"
                                                               Visibility="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </Border>

                <!-- Connect Button -->
                <Button x:Name="ConnectButton" Grid.Column="1" Click="ConnectButton_Click"
                        Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M13,19H14A1,1 0 0,1 15,20H22V22H15A1,1 0 0,1 14,23H10A1,1 0 0,1 9,22H2V20H9A1,1 0 0,1 10,19H11V17H7A1,1 0 0,1 6,16V12A1,1 0 0,1 7,11H17A1,1 0 0,1 18,12V16A1,1 0 0,1 17,17H13V19M7,2H17A2,2 0 0,1 19,4V10H18V4H6V10H5V4A2,2 0 0,1 7,2Z"
                              Fill="{DynamicResource App.Primary.Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                        <TextBlock Text="Connect"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Content - TreeView -->
        <Border BorderThickness="0" CornerRadius="0,0,8,8" Grid.Row="2">
            <TreeView x:Name="ServersTreeView"
                  ItemsSource="{Binding RootNodesServers}"
                  BorderThickness="0"
                  Background="Transparent"
                  Padding="12"
                  SelectedItemChanged="ServersTreeView_SelectedItemChanged"
                  MouseDoubleClick="ServersTreeView_MouseDoubleClick">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TreeViewItem">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Expand/Collapse Toggle -->
                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  Style="{StaticResource ExpandCollapseToggleStyle}"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Margin="0,0,4,0"/>

                                    <!-- Content -->
                                    <ContentPresenter x:Name="PART_Header"
                                                      Grid.Column="1"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>

                                    <!-- Children Items -->
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Margin="10,0,0,0"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="false">
                                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                    </Trigger>
                                    <Trigger Property="HasItems" Value="false">
                                        <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type models:TreeNode}" ItemsSource="{Binding Children}">
                    <Border Padding="8,6" CornerRadius="4" Margin="0,1">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M4,1C2.89,1 2,1.89 2,3V7C2,8.11 2.89,9 4,9H1V11H13V9H10C11.11,9 12,8.11 12,7V3C12,1.89 11.11,1 10,1H4M4,3H10V7H4V3M14,13C12.89,13 12,13.89 12,15V19C12,20.11 12.89,21 14,21H11V23H23V21H20C21.11,21 22,20.11 22,19V15C22,13.89 21.11,13 20,13H14M14,15H20V19H14V15Z"
                                  Fill="{DynamicResource App.Text.Secondary}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="{Binding Server, Converter={StaticResource NullToFontWeightConverter}}"
                                       FontSize="13" Foreground="{DynamicResource App.Text.Primary}"/>
                            <TextBlock Text="{Binding Server.Notes}"
                                       Margin="8,0,0,0"
                                       Foreground="{DynamicResource App.Text.Secondary}"
                                       FontSize="12"/>
                        </StackPanel>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource App.Selection}"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource App.Hover}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
        </Border>
    </Grid>
</UserControl>
